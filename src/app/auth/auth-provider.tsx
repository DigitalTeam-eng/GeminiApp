import { AuthProvider } from './client-auth-provider';
import type { AuthContextType } from './client-auth-provider';

// Re-export the component and type for easy import.
export { AuthProvider };
export type { AuthContextType };
